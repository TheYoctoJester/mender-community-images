header:
  version: 14

  includes:
  - yocto/wrynose/floating/include/tegra-base.yml

repos:
  meta-tegra:
    # refers to the wrynose branch
    branch: wrynose
  meta-tegra-community:
    # refers to the wrynose branch
    branch: wrynose
  meta-tegrademo:
    # refers to the wrynose branch
    branch: wrynose

  meta-mender-community:
    # url and revision are named by the update-scheme include, tegra-classic.yml
    # or tegra-native.yml, which is the only file that knows which branch carries
    # the scheme's layer. Exactly one of them is in any build, so there is one
    # place naming the revision rather than several that have to agree.
    layers:
      meta-mender-tegra/meta-mender-tegra-jetpack7:
