header:
  version: 14

repos:
  meta-raspberrypi:
    url: https://github.com/agherzan/meta-raspberrypi.git
    branch: kirkstone

  meta-lts-mixins:
    url: https://git.yoctoproject.org/meta-lts-mixins
    # Pinned to U-Boot 2024.04 — must match the versioned
    # u-boot_2024.04.bbappend in meta-mender-raspberrypi.
    commit: 66ceeebd047d7fdfc8668b300319a76da8ae257d
    patches:
      kirkstone-compat:
        repo: meta-mender-community
        path: kas/patches/0001-chore-mark-layer-as-compatible-with-kirkstone.patch

  meta-mender-community:
    url: https://github.com/mendersoftware/meta-mender-community.git
    branch: kirkstone
    layers:
      meta-mender-raspberrypi:

local_conf_header:
  raspberrypi: |
    RPI_USE_U_BOOT = "1"
    ENABLE_UART = "1"
    MENDER_BOOT_PART_SIZE_MB = "100"
    IMAGE_INSTALL:append = " kernel-image kernel-devicetree"
    IMAGE_FSTYPES:remove = " rpi-sdimg"
